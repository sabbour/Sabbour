<Page 
    x:Class="TestProject.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    
    xmlns:sabbour="using:Sabbour.WindowsStore.Controls"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Click="Button_Click">Show Fake Active Directory</Button>
            <Button Click="Button_Click_1">Show Fake Progress Bar</Button>
            <Border Height="125" Width="300" BorderThickness="2">
                <Border.BorderBrush>
                    <SolidColorBrush Color="Red"/>
                </Border.BorderBrush>
                <Border.Background>
                    <SolidColorBrush Color="#ffffff"/>
                </Border.Background>
                <sabbour:InkControl/>
            </Border>
        </StackPanel>
    </Grid>
</Page>
